/**
 * Named queue constants — import these instead of hard-coding
 * queue name strings throughout the codebase.
 */
export const QUEUE_NAMES = {
  DELTA_SYNC: 'delta-sync',
  OUTBOUND_CHANGES: 'outbound-changes',
} as const;
